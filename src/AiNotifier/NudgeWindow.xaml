<Window x:Class="AiNotifier.NudgeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        IsHitTestVisible="False"
        SizeToContent="WidthAndHeight"
        Focusable="False"
        ShowActivated="False"
        Opacity="0">

    <Grid Margin="12">
        <!-- Nudge body -->
        <Border x:Name="NudgeBody"
                Background="White" BorderBrush="#e5e7eb" BorderThickness="1"
                CornerRadius="12" Padding="16,10"
                MinWidth="80" MaxWidth="240">
            <Border.Effect>
                <DropShadowEffect BlurRadius="12" ShadowDepth="4" Opacity="0.15" Color="Black"/>
            </Border.Effect>
            <TextBlock x:Name="MessageText"
                       Text=""
                       Foreground="#374151"
                       FontSize="13"
                       TextWrapping="Wrap"
                       TextAlignment="Center"/>
        </Border>
    </Grid>
</Window>
