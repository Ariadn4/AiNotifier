<Window x:Class="AiNotifier.NotificationBubbleWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        SizeToContent="WidthAndHeight"
        Focusable="False"
        ShowActivated="False"
        Opacity="0"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">

    <Grid Margin="12">
        <Border x:Name="BubbleBody"
                Background="White" BorderBrush="#e5e7eb" BorderThickness="1"
                CornerRadius="12" Padding="16,10"
                MinWidth="80" MaxWidth="300"
                Cursor="Hand">
            <Border.Effect>
                <DropShadowEffect BlurRadius="12" ShadowDepth="4" Opacity="0.15" Color="Black"/>
            </Border.Effect>
            <TextBlock x:Name="MessageText"
                       Text=""
                       Foreground="#374151"
                       FontSize="13"
                       TextWrapping="Wrap"
                       TextAlignment="Left"/>
        </Border>
    </Grid>
</Window>
